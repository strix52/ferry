<Window x:Class="Ferry.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Settings" Width="400" SizeToContent="Height"
        WindowStyle="None" ResizeMode="NoResize" ShowInTaskbar="False"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource Surface}" Foreground="{DynamicResource Ink}"
        TextOptions.TextFormattingMode="Ideal"
        FontFamily="Segoe UI Variable Text, Segoe UI" FontSize="13">
    <Border Padding="22,18,22,20">
        <StackPanel>
            <!-- Header -->
            <Grid Margin="0,0,0,16">
                <TextBlock Text="Settings" Style="{StaticResource TitleText}" VerticalAlignment="Center" />
                <Button x:Name="CloseButton" Content="&#xE8BB;"
                        Style="{StaticResource CloseCaptionButton}"
                        HorizontalAlignment="Right" VerticalAlignment="Center"
                        ToolTip="Close" IsCancel="True" />
            </Grid>

            <!-- Appearance Section -->
            <TextBlock Text="APPEARANCE" Style="{StaticResource FaintText}" FontWeight="Bold" Margin="0,0,0,8" />
            <TextBlock Text="Theme" FontWeight="SemiBold" Foreground="{DynamicResource Ink}" />
            <StackPanel Orientation="Horizontal" Margin="0,8,0,16">
                <Button x:Name="ThemeSystemBtn" Content="System" Margin="0,0,8,0" Style="{StaticResource GhostButton}" />
                <Button x:Name="ThemeLightBtn" Content="Light" Margin="0,0,8,0" Style="{StaticResource GhostButton}" />
                <Button x:Name="ThemeDarkBtn" Content="Dark" Style="{StaticResource GhostButton}" />
            </StackPanel>

            <Border Height="1" Background="{DynamicResource Border}" Margin="0,0,0,16" />

            <!-- Device Section -->
            <TextBlock Text="THIS DEVICE" Style="{StaticResource FaintText}" FontWeight="Bold" Margin="0,0,0,8" />
            <TextBlock Text="Device name" FontWeight="SemiBold" Foreground="{DynamicResource Ink}" />
            <TextBlock Text="Shown with your messages." Style="{StaticResource MetaText}" Margin="0,2,0,8" />
            <Grid Margin="0,0,0,16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBox x:Name="DeviceNameBox" MaxLength="40" VerticalAlignment="Center" Margin="0,0,8,0" />
                <Button x:Name="SaveNameButton" Grid.Column="1" Content="Save" Style="{StaticResource GhostButton}" VerticalAlignment="Center" />
            </Grid>

            <Border Height="1" Background="{DynamicResource Border}" Margin="0,0,0,16" />

            <!-- Security Section -->
            <TextBlock Text="SECURITY" Style="{StaticResource FaintText}" FontWeight="Bold" Margin="0,0,0,8" />
            <TextBlock Text="Pairing" FontWeight="SemiBold" Foreground="{DynamicResource Ink}" Margin="0,0,0,8" />
            <Border Background="{DynamicResource Surface3}" BorderBrush="{DynamicResource Border}" BorderThickness="1"
                    CornerRadius="10" Padding="12,10" Margin="0,0,0,16">
                <StackPanel>
                    <TextBlock x:Name="AuthStatusText" Text="Paired" Foreground="{DynamicResource Ink}" FontWeight="SemiBold" />
                    <TextBlock Text="Rotating signs out paired devices."
                               Style="{StaticResource MetaText}" TextWrapping="Wrap" Margin="0,4,0,10" LineHeight="16" LineStackingStrategy="BlockLineHeight" />
                    <StackPanel Orientation="Horizontal">
                        <Button x:Name="RotateTokenButton" Content="Rotate token" Style="{StaticResource DangerButton}" Margin="0,0,8,0" />
                        <Button x:Name="PairPhoneButton" Content="Show QR" Style="{StaticResource GhostButton}" />
                    </StackPanel>
                </StackPanel>
            </Border>

            <Border Height="1" Background="{DynamicResource Border}" Margin="0,0,0,16" />

            <!-- Storage Section -->
            <TextBlock Text="STORAGE" Style="{StaticResource FaintText}" FontWeight="Bold" Margin="0,0,0,8" />
            <Grid Margin="0,0,0,6">
                <TextBlock x:Name="StorageUsedText" Text="0 B" FontWeight="SemiBold" Foreground="{DynamicResource Ink}" />
                <TextBlock x:Name="StorageLimitText" Text="of 2 GB" HorizontalAlignment="Right" Style="{StaticResource MetaText}" />
            </Grid>
            <ProgressBar x:Name="StorageBar" Height="6" Minimum="0" Maximum="100" Value="0" Margin="0,0,0,10" />
            <TextBlock x:Name="StorageCountsText" Text="0 files · 0 messages" Style="{StaticResource MetaText}" Margin="0,0,0,10" />
            <Button x:Name="CleanupButton" Content="Clean up files…" Style="{StaticResource GhostButton}" HorizontalAlignment="Left" />
        </StackPanel>
    </Border>
</Window>
